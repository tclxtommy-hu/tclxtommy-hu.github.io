# 2026-08-21

## Harness 知识库初始化
- 为 `posts/AI/Harness` 下 9 个空文件夹各新增 `README.md`（基本概念信息），分两类：
  - `harness 框架`：AgentScope、AutoGen、CrewAI、LangChain、LlamaIndex Agents
  - `成品 harness`：Aider、Claude Code、Cline、Cursor
- AgentScope 目录额外新增 `AgentScope详细介绍.md`（含多个 mermaid flowchart：总体架构、消息传递 Actor 模型、Pipeline 编排、GroupChat、容错机制、Studio 调试）。
- 文档风格对齐博客既有 README：中文为主+英文术语、blockquote 元信息、表格索引、mermaid 图。
- 注：AgentScope/README.md 与 AgentScope详细介绍.md 已在 README 中互链，并链接到 `../../README.md`（Harness 上层索引，当前不存在，后续可补）。

## Cursor 规则同步至 WorkBuddy
- 将 `.cursor/rules/` 下 3 个 `.mdc` 规则（markdown-bold-spacing、markdown-english-ipa、markdown-abbreviation）原样镜像到 `.workbuddy/rules/`，实现 Cursor 与 WorkBuddy 两端 Markdown 规范统一。
- 同步在 `.workbuddy/memory/MEMORY.md` 新增规范要点索引，便于编辑 `.md` 时自动遵守。

## sandbox沙箱.md 补充多 Harness 沙箱实践
- `posts/AI/Harness/sandbox沙箱.md` 原以 AgentScope Workspace 为主线（八节），新增「九、主流 Agent / Harness 的沙箱实践对照」：9 个 harness 对照表（Claude Code、OpenAI Codex、OpenHands、AutoGen、LangGraph、Devin、Google Jules、Cursor、Aider）+ 10 条可复用最佳实践；原「沙箱不是安全的全部」顺延为「十」。
- 新增缩写表条目 CLI、RBAC、AST、WSL；英文术语首次出现均补 IPA，并修正加粗内冒号的两处 bold-spacing 违规。
- 据用户指正，修正 OpenAI Codex 行：其本质为云端沙箱——模型推理与默认执行均在 OpenAI 云端、每步经 API 往返；本地模式仅可选把命令执行挪回本机，模型推理仍走云端（原「云端沙箱（本地亦跑）/ 本地默认沙箱」表述误导，已改准）。
- 在对照表末尾补 **WorkBuddy** 行：归「本机 + 沙箱」一类（本地执行 + 运行时沙箱 + 托管隔离运行时；`dangerouslyDisableSandbox` 逃生口需用户显式授权），与 Claude Code / Cursor / Aider 同桶，区别于 Codex / Devin / Jules 的云端沙箱 VM。

## sandbox沙箱.md 新增「十、沙箱的一般实现方式」
- 在对照表（九）与安全章（原十）之间插入新章节「十、沙箱的一般实现方式」，安全章顺延为「十一」。新章节按「隔离发生在哪一层」重新分类：进程级（Namespace/seccomp/capabilities/rlimit/chroot）→ 用户态沙箱（bubblewrap/gVisor/Seatbelt/AppContainer）→ 容器（Docker/K8s/containerd/runc）→ 微 VM（Firecracker/Kata/Cloud Hypervisor/QEMU）→ 语言运行时（WASM/V8/Deno/Lua）→ OS 原生框架（SELinux/AppArmor/cgroups/TCC）→ 网络与文件系统隔离 → 硬件机密计算（TEE/SGX），并附取舍表。
- 与第三节「类型谱系」（产品梯度）明确为「正交两套视角」，避免与既有内容重复。
- 已定义术语（gVisor/Seatbelt/Kata/WSL/WASM/Firecracker/Docker/seccomp）不再重复注音标；新术语首次出现补 IPA；文末缩写表新增 TEE、SGX、cgroups、QEMU、rlimit、chroot、TCC 共 7 条。
