---
description: 文档不能单纯讲方法论，每个抽象概念必须配落地示例
globs: "**/*.md"
alwaysApply: true
---

# 文档必须方法论 + 落地示例

写或补本仓库知识库 / 学习路线 / 技术方案类 `.md` 时，**禁止通篇纯方法论**。每讲一个抽象概念、流程或原则，必须紧跟落地示例，让读者「照着就能做」。

## 合格示例至少满足其一

1. **贯穿案例**：虚构但真实感足够的业务场景，带真实数字（如「电商客服『小智』，日 2 万请求，目标 8 周通过率 82% → 85%+」）；
2. **可运行代码**：完整到能直接跑（含 import、类型标注），语言遵守 `code-sample-language`；
3. **具体数据样例**：一条黄金集 YAML、一条偏好对 JSON、一张标注分流实例表——展示数据的「真实样子」，不是抽象分类；
4. **端到端排期**：把方法论排成周级计划 + 工具栈表（如「W1 冷启动 → W2 埋点 → W3 标注 → W4 回归灰度」）。

## 正确

```markdown
### 2.3 回归
（原则描述 2~3 条）

**落地示例**：噪声带测定与回归判定（Node.js + TS）：

（TS 代码块）

代入「小智」的真实数字：
- v1.3 同版连跑三次 → 噪声带 0.6pp，门槛 0.9pp；
- v1.4 跑出 +2.8pp，认定有效；
- 另一场景回退 2.2pp → 回归失败，不发全量。
```

## 错误（不要写）

```markdown
### 2.3 回归
回归测试很重要，要建立基线，关注指标波动，注意跷跷板效应，持续迭代。

（通篇只有原则罗列，无案例、无代码、无数字、无工具）
```

## 范本

`posts/AI/AI工程师学习路线/07-前沿与形态/05-数据飞轮与合成数据.md`——每个方法论章节均含「落地示例」小节，文末附「四周迭代」端到端排期与工具栈速查。

## 要点

1. **原则：示例 = 1**：每一节方法论，至少一个示例跟随；纯概念过渡段可豁免。
2. **数字要真实感**：示例里的指标、阈值、配额写成具体值（82%、0.6pp、500 条），不写「一些」「较多」。
3. **与现有规则共存**：代码示例遵守 `code-sample-language`；加粗、音标、缩写规则照常执行。
