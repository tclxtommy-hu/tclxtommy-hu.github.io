# 中文分词对检索的影响

BM25 的效果高度依赖分词质量，因为词项是倒排索引与打分的基本单元。

## 二元分词

二元分词（bigram）把连续中文切成相邻两个字符的片段，纯 JS 实现、零依赖，对未登录词友好，但会引入大量交叉碎片，召回尚可、精度一般。

## jieba 分词

基于词典与统计的 jieba 能切出更贴近语义的词，显著减少碎片、提升检索精度。本 demo 通过可选依赖 @node-rs/jieba（Rust 预编译）接入，安装后自动启用。

## 英文与通用文本

英文等以空格分隔的语言，可用小写加字母数字切分的 simple 分词器，直接得到天然词项。
