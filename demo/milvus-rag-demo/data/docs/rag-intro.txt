RAG 技术简介

检索增强生成（RAG，Retrieval-Augmented Generation）把「检索」与「生成」结合起来。
回答问题前，先从外部知识库检索相关片段，再把片段交给大模型生成答案。

核心步骤包括：文档加载与清洗、文本分块（Chunking）、Embedding 向量化、
向量存储与检索、提示词编排与生成。

为什么需要 RAG：
- 缓解幻觉：答案有据可依；
- 引入私有或最新知识，无需微调；
- 可解释、可追溯到具体来源片段。

本 Demo 演示前半段流水线：文档切片 → 本地向量化 → Milvus Lite 语义查询。
