---
description: 文章缩写首次出现须附全拼与音标，文末推荐汇总表
globs: "**/*.md"
alwaysApply: true
---

# Markdown 缩写：全拼 + 音标 + 文末汇总

写或改本仓库文章（`.md`）时，**所有缩写**（Acronym / Initialism）在正文中 **首次出现** 须同时给出：

1. **全拼**（英文展开）
2. **音标**（缩写本身 + 全拼，均为 IPA）
3. **中文释义**（可选但推荐）

并 **推荐** 在文章末尾增加「本文缩写」汇总表。

## 正文首次出现（正确）

```markdown
- **ACL** /ˌeɪ siː ˈel/ （ **Access Control List** /ˈækses kənˈtrəʊl lɪst/ ，访问控制列表）
- **RBAC** /ˌɑːr biː eɪ ˈsiː/ （ **Role-Based Access Control** /rəʊl beɪst ˈækses kənˈtrəʊl/ ，基于角色的访问控制）
```

后文再次写 ACL、RBAC 时不必重复全拼与音标。

## 错误（不要写）

```markdown
- **ACL** （显式名单）
首次出现无全拼、无音标。

- 使用 ACL 做过滤
正文首次直接甩缩写，读者不知道展开。
```

## 文末汇总（推荐）

技术文、概念文建议在文末加一节（标题可用「本文缩写」/「缩写对照」），用表格列出本文出现过的缩写：

```markdown
## 本文缩写

| 缩写 | 音标 | 全拼 | 中文 |
|------|------|------|------|
| **ACL** | /ˌeɪ siː ˈel/ | Access Control List | 访问控制列表 |
| **RBAC** | /ˌɑːr biː eɪ ˈsiː/ | Role-Based Access Control | 基于角色的访问控制 |
```

- 表中全拼若已在正文给过音标，汇总表可只保留全拼单词，或再次附全拼音标（任选，全文一致即可）。
- 仅代码标识符、文件名、命令中的缩写不必强行展开；一旦进入正文叙述，仍按首次出现规则处理。

## 要点

1. **所有缩写**：含 ACL、RBAC、IdP、JWT 等；不以「大家都认识」为由省略全拼。
2. **仅首次**：同一缩写在本文后续出现不再附全拼/音标。
3. **标题已出现**：正文首次仍应完整展开一次。
4. **与音标规则配合**：缩写、全拼的音标格式遵守 `markdown-english-ipa`；加粗留白遵守 `markdown-bold-spacing`。
5. **文末表推荐**：缩写 ≥ 2 个时强烈建议加汇总表；只有 1 个也可加，便于检索。
