---
description: Markdown 加粗两侧必须留空格，否则本站渲染不生效
globs: "**/*.md"
alwaysApply: true
---

# Markdown 加粗空格规则

写或改本仓库任意 `.md` 时，`**加粗**` 两侧必须有空格，否则构建后的渲染不会处理加粗。

## 正确

```markdown
以 **会话** 为基本单位
ChatFlow 以 **对话体验** 为中心
- **定位** ：说明文字
1. **触发** ：下一步
原则： **前台对话短链路、可解释。**
```

## 错误（不要写）

```markdown
以**会话**为基本单位
ChatFlow 以**对话体验**为中心
- **定位**：说明文字
面向**对话型应用**的编排
```

## 要点

1. 开标签 `**` 前面有空格（行首/列表标记后已有空格除外，如 `- **x**`）。
2. 闭标签 `**` 后面有空格（包括中文冒号 `：`、句号等标点前也要空格）。
3. 表格单元格 `| **n8n** |` 两侧已有空格即可，不要贴着汉字写 `字**加粗**字`。
